# Automaker - Coolify Deployment Configuration
# ============================================
#
# This setup uses Claude Code CLI authentication (your Claude Pro/Max subscription)
# No Anthropic API key required!
#
# SETUP STEPS:
# 1. SSH into your Coolify server
# 2. Run: npm install -g @anthropic-ai/claude-code
# 3. Run: claude login
# 4. Note the path to ~/.claude (usually /root/.claude or /home/username/.claude)
# 5. Configure the volume mount in Coolify (see docker-compose.coolify.yml)

# ============================================
# OPTIONAL - API Key (not needed if using CLI auth)
# ============================================

# Only set this if you want to use an Anthropic API key instead of CLI auth
# ANTHROPIC_API_KEY=sk-ant-...

# ============================================
# RECOMMENDED FOR PRODUCTION
# ============================================

# API key for authenticating requests to Automaker's backend
# Generate a secure random string (e.g., openssl rand -hex 32)
AUTOMAKER_API_KEY=

# CORS origin - your Coolify domain
# Set to your actual domain (e.g., https://automaker.yourdomain.com)
CORS_ORIGIN=http://localhost:3007

# ============================================
# OPTIONAL - Security
# ============================================

# Root directory for projects (inside container)
# Default: /projects
ALLOWED_ROOT_DIRECTORY=/projects

# ============================================
# OPTIONAL - Terminal Access
# ============================================

# Enable/disable terminal access (default: true)
TERMINAL_ENABLED=true

# Password to protect terminal access
# Recommended for production!
TERMINAL_PASSWORD=

# ============================================
# OPTIONAL - Server
# ============================================

# Server port (default: 3008)
PORT=3008

# Data directory (inside container)
DATA_DIR=/data

# HTTP request logging (default: false)
ENABLE_REQUEST_LOGGING=false

# ============================================
# OPTIONAL - GitHub Integration
# ============================================

# GitHub token for git operations
# Get with: gh auth token
# GH_TOKEN=ghp_...
